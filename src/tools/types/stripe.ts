/**
 * Stripe API TypeScript Interfaces
 * (types derived from stripe package are used inline)
 */

export interface StripeToolParams {
  limit?: number;
  email?: string;
  name?: string;
  description?: string;
}